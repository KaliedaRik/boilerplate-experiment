const whereAmI = 'STACK environment';
const whatAmI = 'api credentials';

const siteUrl = '';										// website frontend domain eg: 'www.example.com'
const siteProtocol = '';								// 'http' or 'https'

const defaultLocale = 'en';								// default locale code eg: 'en'
const localeList = [];									// array of locale codes accepted by the api eg: ['en', 'xp']

export {
	whereAmI,
	whatAmI,
	siteUrl,
	siteProtocol,
	defaultLocale,
	localeList
}
